# 完整博客建设需求文档

## 1. 文档说明

本文用于规划一个可长期运营的个人博客、团队博客或内容站点。文档同时覆盖最小可行版本（MVP）和完整版本，开发时可以按阶段交付。

## 2. 项目目标

### 2.1 核心目标

- 提供稳定、快速、易读的文章发布与阅读体验。
- 让作者能够高效管理文章、分类、标签、评论和媒体资源。
- 通过搜索、归档、相关推荐和订阅，帮助读者持续发现内容。
- 具备 SEO、无障碍、备份、安全和可观测性能力，支持长期运营。

### 2.2 非目标

- 不在第一阶段实现完整社交网络、即时聊天或复杂电商能力。
- 不为了功能数量牺牲首屏性能、内容可读性和数据安全。

## 3. 用户角色与权限

| 角色 | 主要权限 |
| --- | --- |
| 访客 | 浏览文章、分类、标签、归档、搜索、发表评论（如开启） |
| 注册用户 | 管理个人资料、查看评论状态、收藏或订阅（如开启） |
| 作者 | 创建、编辑、预览、发布和管理自己的文章及媒体 |
| 编辑 | 审核文章、管理全站内容、处理评论和举报 |
| 管理员 | 用户、权限、站点配置、备份、日志和所有数据管理 |

权限应遵循最小授权原则，并记录关键管理操作。

## 4. 用户端功能

### 4.1 博客首页 `/blog`

- 展示站点名称、简介、导航和搜索入口。
- 展示置顶文章、最新文章、热门文章或精选专题。
- 显示文章发布时间、更新时间、作者、分类、标签、摘要和阅读时长。
- 提供分页或“加载更多”，避免一次加载过多内容。

### 4.2 文章详情页

- 支持 Markdown 或富文本内容，至少支持标题、段落、列表、引用、代码块、表格、图片、视频和链接。
- 展示标题、封面、作者、发布时间、更新时间、分类、标签和阅读量。
- 自动生成目录，支持锚点跳转；长文章提供返回顶部。
- 支持代码高亮、代码复制、图片放大、外部链接安全提示。
- 支持上一篇/下一篇、相关文章、分享链接和打印样式。
- 文章不存在、未发布或无权访问时，返回友好的状态页。

### 4.3 分类、标签与归档

- 分类支持层级结构、描述和封面（可选）。
- 标签支持列表、标签详情和文章数量统计。
- 按年、月展示文章归档。
- 提供站点地图、RSS/Atom Feed 和文章订阅入口。

### 4.4 搜索

- 支持按标题、正文、分类和标签搜索。
- 展示关键词、结果数量、摘要和高亮匹配内容。
- 无结果时给出建议；输入非法或过长时进行校验。
- 内容规模较小时可使用数据库全文检索，规模扩大后再接入专用搜索引擎。

### 4.5 评论与互动（可配置）

- 支持评论、回复、点赞或收藏等互动功能。
- 评论支持审核、折叠、删除、举报和反垃圾策略。
- 评论者可填写昵称、邮箱和网站；邮箱默认不公开。
- 提供评论通知和隐私说明。

### 4.6 关于、联系与隐私页面

- 关于页面：作者或团队介绍、技术栈、联系方式和版权声明。
- 联系页面：表单、公开邮箱或社交链接，表单具备验证码和限流。
- 隐私政策、Cookie 说明、用户协议（如涉及账户或订阅）。

### 4.7 响应式与无障碍

- 适配手机、平板和桌面端，正文宽度和行高优先保障阅读体验。
- 支持键盘操作、清晰焦点状态、语义化 HTML、图片替代文本和足够色彩对比度。
- 支持深色模式（可选），用户偏好应持久化。

## 5. 管理端功能

### 5.1 登录与账户

- 管理员登录、退出、修改密码和找回密码。
- 可选启用双因素认证、登录验证码和登录失败锁定。
- 用户列表、角色分配、启用/禁用、会话失效和操作日志。

### 5.2 文章管理

- 新建、编辑、预览、保存草稿、定时发布、发布、下线、删除和恢复。
- 支持草稿、审核中、已发布、已下线、回收站等状态。
- 支持 Markdown 原文、所见即所得编辑器或二者切换。
- 支持自动保存、版本历史、差异对比和草稿恢复。
- 支持文章摘要、封面、SEO 标题、SEO 描述、规范链接和置顶设置。
- 支持批量修改分类、标签、状态和作者。

### 5.3 分类与标签管理

- 增删改查、排序、合并、别名和删除前引用检查。
- 删除分类或标签时明确处理关联文章，避免产生孤立数据。

### 5.4 媒体库

- 上传、预览、搜索、重命名、替换和删除图片、视频、音频及附件。
- 图片支持压缩、缩略图、WebP/AVIF（按浏览器能力）和尺寸校验。
- 记录文件大小、类型、宽高、上传者和引用位置。
- 限制扩展名与真实 MIME 类型，禁止上传可执行脚本。

### 5.5 评论与反馈

- 评论审核队列、批量通过/拒绝/删除、关键词过滤、举报处理。
- 联系表单消息列表、状态、备注和导出（如需要）。

### 5.6 站点配置

- 站点名称、Logo、描述、语言、时区、域名、分页数量、评论开关。
- 导航菜单、页脚链接、社交账号、统计代码、公告和维护模式。
- SEO 默认模板、robots.txt、站点地图和 RSS 配置。

### 5.7 数据看板

- 文章数量、草稿数量、评论数量、访问量、热门文章和错误数。
- 统计应明确数据来源、时区和去重口径，避免把统计数字当作绝对精确值。

## 6. 页面清单

### 6.1 公共页面

- 站点介绍页/入口页（`/`）
- 博客首页（`/blog`）
- 文章详情页
- 分类列表页与分类详情页
- 标签列表页与标签详情页
- 年/月归档页
- 搜索结果页
- 作者页
- 关于页
- 联系页
- 隐私政策页、用户协议页
- 404、403、500、维护中页面

### 6.2 管理页面

- 登录页
- 仪表盘
- 文章列表、编辑器、预览和版本页
- 分类与标签页
- 媒体库
- 评论与举报页
- 用户与角色页
- 站点设置页
- 操作日志、任务状态和备份页

## 21. 视觉风格与页面调性

### 21.1 整体定位

站点应呈现“有个人气质的内容空间”，而不是模板化的信息流或营销落地页。视觉重点依次是：作者/品牌识别、内容主题、阅读效率和长期可信度。

建议关键词：克制、清晰、温度、编辑感、可持续。页面需要有明确的视觉层次，但不依赖大量卡片、渐变背景、装饰性光球或复杂动画制造所谓的高级感。

### 21.2 色彩与材质

- 使用一组中性背景色配合一个明确的品牌强调色，再用暖色或绿色作为少量状态色，避免全站只有单一蓝色或紫色。
- 正文背景与文字保持高对比度；代码块、引用、提示和危险操作使用不同的语义色。
- 允许使用纸张、胶片、网格或轻微噪点等材质表达内容气质，但必须保证文字和交互控件清晰。
- 深色模式不是简单反转颜色：代码块、图片边框、分割线、悬停状态和阴影都需要单独校准。

### 21.3 字体与排版

- 标题使用具有识别度的显示字体或稳重的衬线字体，正文使用高可读性的无衬线/宋体组合；中文与英文回退字体要明确。
- 不使用随视口宽度连续缩放的字体。标题、正文、辅助文字采用稳定的响应式档位。
- 正文建议 680-760px 最大宽度，行高 1.75-2.0；管理后台使用更紧凑的行高以提升扫描效率。
- 标题、摘要、元信息、正文、代码和引用必须有清晰的字号与间距层级，避免每块内容都使用同样的视觉重量。

### 21.4 布局与组件

- 公共页面采用宽松的内容容器和少量分区，优先使用整段内容区域，不把每个区块都包成浮动卡片。
- 重复文章使用低圆角或无圆角列表项；真正需要聚焦的文章、媒体预览和对话框才使用卡片容器。
- 按钮、标签、状态、分页、输入框和表格遵循统一尺寸与焦点样式；图标使用统一图标库，陌生图标必须有 tooltip。
- 图片使用真实的作者、作品、地点或内容相关视觉素材；没有合适图片时使用有明确内容含义的生成位图或品牌纹理。

### 21.5 动效与声音

- 页面切换、弹窗、目录高亮和复制成功使用 150-250ms 的轻量过渡，不使用阻碍阅读的连续动画。
- 介绍页可以有一次性的进入动效，但“进入博客”必须在首次渲染后立即可操作。
- 尊重 `prefers-reduced-motion`；站点默认不自动播放声音，视频和动效提供暂停方式。

### 21.6 风格验收

- 任一页面第一屏都能看出当前页面用途和下一步操作。
- 手机 375px 宽度下无横向滚动，标题、按钮、标签和卡片文字不溢出。
- 页面截图在浅色和深色模式下都具备可读对比度；焦点状态和键盘路径可见。
- 视觉装饰不得遮挡标题、摘要、二维码、导航或主要操作。

## 22. 站点介绍页/入口页 `/`

### 22.1 页面目的

根路径不直接展示文章列表，而是作为站点的第一印象和内容入口。访客先了解“这是一个什么空间、由谁维护、值得阅读什么”，再通过明确的 CTA 进入博客。

### 22.2 首屏结构

- 全宽首屏，左侧或上方展示站点名称/作者名称和一句具有观点的定位语。
- 中央区域展示真实的主题视觉：作者工作台、代表作品、城市/旅行照片、主题插画或其他与博客内容直接相关的位图素材。
- 视觉层上叠加简短介绍，不使用“文字卡片 + 图片卡片”的营销分栏结构。
- 主操作按钮：“进入博客”或“开始阅读”；次操作：“了解作者”“查看精选”。
- 首屏底部露出下一段内容的少量边缘，例如精选文章标题或内容主题，让用户知道页面可以继续浏览。

### 22.3 内容区块

| 区块 | 内容 | 作用 |
| --- | --- | --- |
| 站点宣言 | 1-2 句作者/团队真实表达 | 建立记忆点，不堆叠口号 |
| 内容方向 | 3-5 个主题，如工程实践、阅读、摄影 | 帮助访客判断是否有兴趣 |
| 精选文章 | 3 篇以内，展示标题、摘要、阅读时长 | 让访客看到实际内容质量 |
| 作者简介 | 头像、姓名、所在地/职业、简短经历 | 建立可信度和人际感 |
| 更新节奏 | 最近更新时间、订阅入口或 RSS | 告知站点是否仍在维护 |
| 页脚 | 进入博客、关于、联系、隐私和社交链接 | 提供完整导航和合规入口 |

### 22.4 交互与实现结果

| 功能 | 操作 | 结果 |
| --- | --- | --- |
| 进入博客 | 点击主按钮或按 Enter | 进入 `/blog`，页面顶部显示博客导航；记录入口来源用于返回 |
| 查看精选文章 | 点击文章卡片/标题 | 进入 `/blog/posts/:slug`，浏览器返回可回到介绍页原滚动位置 |
| 了解作者 | 点击次按钮或滚动到作者区 | 平滑滚动到作者简介；直接访问锚点 URL 也能定位 |
| 查看主题 | 点击主题标签 | 进入对应 `/blog/categories/:slug` 或 `/blog/tags/:slug` |
| 跳过介绍 | 点击“直接阅读”文字链接 | 立即进入 `/blog`，不弹确认框；链接在键盘和移动端都可用 |
| 主题切换 | 点击明暗模式 | 介绍页即时切换，图片和文字对比度保持可读 |
| 分享站点 | 点击站点分享按钮 | 分享站点介绍页的标题、摘要、封面和 URL，不误分享后台或博客筛选链接 |

### 22.5 首次访问与回访策略

- 默认根路径始终是介绍页，保证新访客获得完整的站点背景。
- 点击“进入博客”后记录本地 `intro_seen` 标记；再次访问根路径时可显示更短的“快速入口”状态，但不强制跳过介绍页。
- 如果用户直接访问 `/blog`、文章链接、搜索链接或外部分享链接，不经过介绍页，优先完成用户当前任务。
- 介绍页加载失败时，仍提供纯 HTML 的站点名称、简介和“进入博客”链接，不能把用户困在空白页。
- 不使用强制延时或自动跳转；访客无需等待动画结束即可点击进入博客。

### 22.6 介绍页体验小巧思

- 主按钮旁显示“最新更新：YYYY-MM-DD”，让入口页有持续更新感，而不是一次性宣传页。
- 精选文章卡片显示“已读/未读”仅在本地记录，回访时可提示“继续阅读”，不上传阅读历史。
- 鼠标悬停或触摸精选文章时只改变标题颜色和轻微位移，不让整块图片剧烈放大。
- 在首屏视觉下方放置一条细窄的内容目录/主题导航，滚动时变为吸顶导航，方便进入博客前先探索。
- 对低带宽设备提供静态封面和较少动效；图片加载失败时显示与内容相关的纯色背景和 alt 文本。

### 22.7 介绍页后台配置

- 配置入口页标题、定位语、主视觉、主按钮文字/URL、次按钮、精选文章、主题方向、作者简介和更新日期。
- 支持桌面图、移动图和默认降级图；上传时校验尺寸、大小、真实 MIME 类型和替代文本。
- 支持预览浅色/深色模式、桌面/移动端、首次访问/回访状态。
- 保存后显示生效范围和缓存刷新状态；精选文章被删除或下线时提示管理员替换，不静默显示空卡片。

### 22.8 介绍页验收标准

- 访问 `/` 时第一屏不是文章列表，而是具有站点名称、主题视觉、简介和“进入博客”按钮的介绍页。
- 点击“进入博客”后进入 `/blog`，博客首页能正常展示文章、导航、搜索和分类入口。
- 直接打开文章分享链接时可以直接阅读文章，不被介绍页拦截。
- 介绍页在 375px、768px 和桌面宽度下布局稳定，主标题、按钮和图片无重叠或溢出。
- 介绍页在图片接口、统计脚本或动效失败时仍能进入博客。

## 23. 参考 landing.love 的设计方向

参考来源：[landing.love](https://www.landing.love/)。这里只提炼布局和交互方法，不直接复制其截图、品牌、文案、动画代码或第三方素材；最终视觉应围绕博客作者和文章内容重新设计。

### 23.1 可借鉴的设计方法

- **首屏先说清楚定位**：使用一个有观点的主标题和一段短说明，让访客先理解站点主题，再进入内容。
- **极简全局导航**：保留站点标识、内容入口、关于/联系、搜索和主题切换，把不常用入口收进菜单。
- **主题筛选导航**：参考其分类入口，将博客主题做成一组可扫描的文本标签或细线按钮，点击后进入博客筛选结果。
- **视觉内容先行**：介绍页使用一张或一组真实的全幅视觉素材，精选文章使用稳定比例的封面预览，让页面具有记忆点。
- **内容网格而非功能堆叠**：精选文章可以使用不等宽或错落网格，但每个项目都要有标题、摘要和明确的进入行为。
- **低干扰动态**：允许使用滚动出现、图片轻微位移、标题逐字显现等一次性动效，但不能延迟 CTA 或影响阅读。
- **页脚保持完整**：提供 RSS、社交链接、隐私和联系入口，形成从灵感浏览到实际阅读的完整闭环。

### 23.2 应用于本博客介绍页的具体方案

#### 方案 A：编辑杂志感（推荐）

- 背景使用明亮中性色，品牌强调色用于按钮、链接和分类标签。
- 首屏左上显示站点名，中央大标题显示博客的核心主题，右侧/下方使用一张作者工作台或代表作品图片。
- 下方以“主题标签 + 最近更新”形式引导探索；精选文章采用两列错落布局。
- 适合技术写作、读书、设计、摄影等需要长期沉淀和阅读的博客。

#### 方案 B：作品集展厅感

- 首屏使用深色背景和高对比度图片，展示一件代表作品或一组内容切片。
- CTA 使用细边框按钮和明确的方向箭头，进入博客后恢复浅色/深色阅读主题。
- 精选文章以大图和短标题为主，摘要在悬停或展开时出现；移动端必须默认显示摘要，不能依赖悬停。
- 适合视觉创作者、摄影师、设计师或有较强作品展示属性的个人站点。

#### 方案 C：内容地图感

- 首屏使用网格、时间轴或索引式排版，直接展示“我写什么”和“最近更新什么”。
- 主题标签带文章数量，进入博客后自动带上筛选条件。
- 精选文章使用编号、日期和阅读时间，强调持续记录而非宣传。
- 适合工程实践、研究笔记、知识库和团队技术博客。

### 23.3 不应照搬的部分

- 不直接使用 landing.love 的 Logo、截图、站点名称、分类数量或案例文案。
- 不为了追求“案例感”加入大量 3D 场景、视频背景或 WebGL；只有确实服务于博客主题且性能可控时才使用。
- 不把介绍页做成只有一句口号和一个按钮的空壳；首屏下方必须露出真实内容线索。
- 不使用大面积渐变光球、漂浮装饰、自动播放声音或必须等待完成的加载动画。
- 不让分类标签看起来像广告筛选器；标签应表达博客真实内容，并能跳转到可用的文章列表。

### 23.4 案例参考后的视觉验收

- 首屏在 3 秒内能回答“这是哪个站点、作者写什么、下一步去哪里”。
- 视觉素材与博客主题有直接关系，不使用模糊、过度裁剪或无法辨识内容的氛围图。
- 分类标签、精选文章和主按钮均可点击、可键盘访问，并有明确的悬停/焦点状态。
- 动效关闭后，页面信息层级和所有核心操作仍然完整。
- 介绍页和博客页属于同一品牌系统，但博客页会自动收敛视觉装饰，优先保证长文阅读。

## 24. 科技博客介绍页首屏设计方案

### 24.1 设计概念：Signal Room

首屏的感觉像一间正在工作的“技术信号室”：访客先看到一个真实、可辨认的科技视觉，再看到作者正在研究和记录什么，最后进入文章内容。它应有设计感和动效，但信息顺序必须始终清楚：

1. 这是一个什么站点。
2. 作者主要写什么。
3. 最新内容是什么。
4. 如何进入博客。

科技感来自精确的排版、数据标签、微型状态信息和克制的动态细节，而不是满屏霓虹、无意义的代码雨、复杂粒子或大面积渐变。

### 24.2 首屏构图

首屏高度建议为 `min(860px, 100svh)`，全幅视觉铺满视口。文字直接叠加在视觉上，不使用“左文字卡片 + 右图片卡片”的分割式英雄区。

```text
┌─────────────────────────────────────────────────────────────┐
│ [LOGO / BLOG NAME]       ARTICLES  TOPICS  ABOUT   [theme]  │
│                                                             │
│  [LIVE / NOTES 2026]                                        │
│                                                             │
│  Technology, explained                                      │
│  slowly and clearly.                         [主题视觉]     │
│  关于软件、AI、系统与数字世界的长期记录。                  │
│                                                             │
│  [进入博客  →]   [查看精选]                                  │
│                                                             │
│  NOW READING / 03   LATEST NOTE: ...        scroll to explore │
└─────────────────────────────────────────────────────────────┘
```

#### 首屏区域说明

| 区域 | 内容 | 设计要求 |
| --- | --- | --- |
| 顶部导航 | Logo、文章、主题、关于、主题切换 | 透明或半透明，首屏滚动后转为实色吸顶；不覆盖主标题 |
| 状态眉标 | `TECH NOTES / 2026`、`ONLINE` 或最近更新日期 | 小字号等宽字体，作为内容语境，不喧宾夺主 |
| 主标题 | 站点主张，例如“把复杂技术讲清楚” | 2-3 行以内；是内容定位，不使用空泛的“欢迎来到我的博客” |
| 辅助说明 | 介绍写作范围和阅读方式 | 2 行以内，给出具体主题：AI、软件工程、系统、工具等 |
| 主 CTA | “进入博客” | 高对比度实心按钮，点击后进入 `/blog` |
| 次 CTA | “查看精选”或“了解作者” | 低强调按钮，滚动到下方精选区或作者区 |
| 主题视觉 | 与科技内容相关的真实/生成位图 | 视觉主体清晰可辨，不使用模糊背景或纯装饰抽象图 |
| 底部状态条 | 最近更新、文章数量、滚动提示 | 提供探索线索；不显示虚构的实时数据 |

### 24.3 推荐视觉资产

主视觉选择下列方向之一，具体内容由作者实际素材决定：

- **工作台纪录**：真实拍摄的屏幕、键盘、开发板、笔记和光线，强调“正在研究”。
- **系统剖面**：生成一张清晰的硬件/芯片/服务器机架宏观图，主体有明确边缘和纹理，强调“系统内部”。
- **数据仪器**：真实的示波器、终端、网络设备或实验设备局部，强调“观察和测量”。
- **城市与网络**：夜间机房、光纤、轨道或城市基础设施，但必须能看出具体对象，不使用纯氛围照片。

图片规格：桌面端至少 2400px 宽，移动端提供独立裁剪图；主对象留出文字叠加安全区；同时准备默认降级图、alt 文本和无动画版本。

### 24.4 动效设计

动效分为“入场、持续、交互、滚动”四层。任何一层失败都不能影响进入博客。

#### 入场时间线

| 时间 | 动作 | 结果 |
| --- | --- | --- |
| 0-150ms | 背景图和页面底色出现 | 首屏立即有稳定视觉，不显示空白等待 |
| 150-450ms | 导航从透明变为可读状态 | Logo 和导航可操作 |
| 300-700ms | 眉标、主标题按区块淡入并轻微上移 | 文字有层次，但不逐字打字造成阅读等待 |
| 550-900ms | 主 CTA 和次 CTA 出现 | 900ms 内可点击进入博客 |
| 700-1200ms | 主题视觉的局部细节轻微移动/聚焦 | 动效结束后停在稳定状态，不无限吸引注意力 |

#### 持续动效

- 视觉素材只做低幅度的镜头漂移、景深变化或局部扫描，周期 8-16 秒；不连续跳动。
- 状态眉标的指示点最多以 2 秒周期呼吸，不使用高频闪烁。
- 底部“向下探索”指示每 3 秒轻微移动一次，用户开始滚动后停止。
- 不自动播放声音；如果素材包含视频，默认静音、循环、提供暂停按钮和静态封面。

#### 交互动效

- 主 CTA 悬停时只增加箭头位移 4-6px 和边框/背景对比，点击后显示 100-150ms 按压反馈。
- 主题标签悬停显示细线或下划线，不能放大到改变布局。
- 导航主题切换使用 180-220ms 颜色过渡；切换后焦点保留在按钮。
- 鼠标移动产生的视差最大 8px；触摸设备、键盘操作和低性能设备关闭视差。

#### 滚动动效

- 首屏滚动到 60% 时，导航从覆盖式变为实色吸顶，并显示“进入博客”小入口。
- 主视觉随滚动最多缩小 4%，文字逐渐降低透明度，但不能与下一段内容重叠。
- 进入精选文章区时，每组内容只触发一次淡入；滚回顶部不重复播放。
- `prefers-reduced-motion: reduce` 时移除漂移、视差和延迟，保留颜色/焦点变化。

### 24.5 首屏内容示例

文案应根据作者实际定位替换，以下仅作为内容结构示例：

```text
TECH NOTES / 2026

把复杂技术讲清楚
关于 AI、软件工程、系统与数字世界的长期记录。

[进入博客 →]  [查看精选]

最近更新：2026-08-15    文章：42 篇
```

文案规则：

- 主标题不超过 18 个中文字符或 8-10 个英文单词，避免首屏变成大段说明。
- 辅助说明必须包含至少两个实际内容方向，不写“探索无限可能”这类不可验证表达。
- CTA 使用动作词，避免“了解更多”这种无法预期结果的按钮。
- 如果显示文章数、日期或在线状态，必须来自真实数据；接口失败时隐藏该字段而不是显示 0。

### 24.6 点击与跳转结果

| 操作 | 跳转/变化 | 体验要求 |
| --- | --- | --- |
| 点击“进入博客” | 进入 `/blog`，保留入口来源参数（可选） | 过渡不超过 400ms；博客首页完成后焦点落在页面标题或内容列表 |
| 点击“查看精选” | 平滑滚动到精选文章区并更新 URL 锚点 | 键盘用户能感知目标标题；不依赖鼠标悬停 |
| 点击“了解作者” | 滚动到作者介绍或进入 `/about` | 目标区顶部留出吸顶导航高度 |
| 点击主题标签 | 进入对应分类/标签详情 | URL 可复制、刷新后条件仍保留 |
| 点击 Logo | 在介绍页回到 `/`；博客内回到 `/blog` | 不让博客读者每次都重新经过介绍页 |
| 点击最近更新 | 打开最近发布的文章 | 文章不存在时回退到博客首页并显示提示 |

### 24.7 响应式设计

#### 桌面端（≥ 1024px）

- 视觉主体占据首屏 55%-70% 的可视面积，文字安全区保持 40%-45% 的宽度。
- 导航横向展示；主题标签和状态条置于主标题下方或首屏底部。
- 允许使用轻微视差和鼠标跟随，但主标题和 CTA 的位置必须稳定。

#### 平板端（768-1023px）

- 视觉主体向背景中央收拢，文字安全区变为 60%-70% 宽度。
- 导航减少为 Logo、搜索、菜单按钮；主题标签移到首屏下方。
- 关闭高成本视频，优先使用静态图或低帧率动效。

#### 手机端（≤ 767px）

- 使用独立竖版图片，文字放在对比度最稳定的区域；不强行把桌面版图片缩放裁切。
- 首屏高度使用 `100svh`，但主标题、CTA 和下一段内容必须在可视范围内有合理呼吸空间。
- 顶部只保留 Logo、菜单和主题切换；菜单打开后覆盖层不遮挡关闭按钮。
- 主 CTA 保持至少 44px 高，底部操作区避开刘海和手势安全区。
- 动效默认为低强度；滚动后立即停止首屏持续动画，减少耗电和眩晕。

### 24.8 技术实现边界

- 首选 CSS transform/opacity 和已压缩的位图动画；避免用大量 DOM 元素模拟粒子或代码雨。
- 如果使用 Canvas/WebGL，只负责背景视觉，不承载标题、按钮、二维码或核心导航。
- 首屏关键文字和 CTA 必须使用真实 HTML，保证 SEO、键盘访问和 JavaScript 失败时仍可用。
- 图片使用 `picture`/响应式尺寸、现代格式和固定宽高比；加载前显示占位以避免 CLS。
- 首屏第三方统计、字体和视频不能阻塞主标题和“进入博客”按钮的可用时间。

### 24.9 首屏验收清单

- [ ] 首屏 900ms 内可操作，入口页不依赖动画结束。
- [ ] 一眼能识别站点名称、科技内容方向和“进入博客”操作。
- [ ] 主视觉是可辨认的科技主题素材，且桌面/移动裁剪都不破坏主体。
- [ ] 动效有入场、持续、滚动和减少动效四种状态；没有闪烁、自动声音或强制等待。
- [ ] `/`、`/blog`、`/about` 和文章链接的导航关系清晰，浏览器返回行为符合预期。
- [ ] 375px 手机视口无溢出、无遮挡，CTA、菜单和主题切换可触控。
- [ ] 键盘 Tab 顺序为：Logo → 导航 → 主标题关联操作 → CTA → 主题/分享；焦点清晰可见。
- [ ] 关闭 JavaScript、图片加载失败或网络变慢时仍能进入博客。

## 7. 推荐数据模型

至少包含以下实体：

- `users`：用户、密码哈希、角色、状态、最后登录时间。
- `posts`：标题、slug、摘要、正文、封面、状态、作者、发布时间、更新时间、SEO 字段。
- `categories`：名称、slug、父分类、描述、排序。
- `tags`：名称、slug、描述。
- `post_tags`：文章与标签的关联关系。
- `media`：文件路径、类型、大小、尺寸、替代文本、上传者。
- `comments`：文章、用户或访客信息、父评论、内容、状态、审核信息。
- `post_revisions`：文章版本、编辑者、版本内容、创建时间。
- `settings`：站点配置键值及版本。
- `subscriptions`：订阅邮箱、确认状态、退订标记（如开启）。
- `share_configs`：文章分享图模板、标题/摘要覆盖、背景图、二维码文案和配置版本（如开启）。
- `share_events`：文章、分享平台、模板、日期和匿名设备类型，用于聚合统计（如开启）。
- `audit_logs`：操作者、动作、目标、IP、时间和结果。

建议对 `slug`、发布时间、状态、分类、标签关联和搜索字段建立索引；所有时间统一存储 UTC，展示时转换为站点时区。

## 8. 技术与架构要求

### 8.1 推荐架构

- 前端：选择团队熟悉的 React/Vue/原生服务端渲染方案，优先保证 SEO 和首屏速度。
- 后端：提供清晰的 REST 或 GraphQL API，统一错误格式和鉴权中间件。
- 数据库：PostgreSQL 或 MySQL；使用迁移工具管理表结构。
- 文件存储：对象存储或独立媒体服务，数据库只保存元数据和地址。
- 缓存：对站点配置、热门文章、分类列表等低频变化数据设置可失效缓存。
- 部署：开发、测试、生产环境分离，配置通过环境变量注入。

### 8.2 内容与 URL 规范

- 文章 URL 使用稳定、可读且唯一的 slug，例如 `/posts/example-title`。
- slug 变更时建立 301 重定向，避免历史链接失效。
- 正文内容与展示层解耦，避免把大量 HTML 直接拼接进模板。
- 用户输入统一转义；Markdown 渲染需要 HTML 白名单和 XSS 防护。

## 9. SEO、性能与可观测性

### 9.1 SEO

- 每页有唯一 title、description、canonical 和 Open Graph/Twitter Card 信息。
- 生成 `sitemap.xml` 和 `robots.txt`，文章页面使用语义化结构化数据（Article）。
- 处理重复内容、分页 canonical、图片 alt、语言和时区信息。

### 9.2 性能目标

- 生产环境启用 HTTPS、压缩、缓存和 CDN（如适用）。
- 首屏关键内容优先加载，图片懒加载，避免阻塞渲染的第三方脚本。
- 目标参考：移动端 Lighthouse Performance 不低于 85，核心页面 LCP 小于 2.5 秒（以真实监控为准）。

### 9.3 监控

- 记录应用错误、慢请求、登录异常、后台任务和外部服务失败。
- 配置错误告警、可用性探测、磁盘/数据库容量告警。
- 区分访问分析和业务日志，遵守隐私与最小采集原则。

## 10. 安全、合规与备份

- 密码使用 Argon2id 或 bcrypt 哈希，禁止明文存储。
- 管理端启用 CSRF 防护、XSS 防护、SQL 参数化、CSP 和安全响应头。
- 文件上传校验扩展名、MIME、大小和内容；下载附件时设置安全响应头。
- 登录、搜索、评论、联系表单和上传接口限流。
- 重要操作需要二次确认，删除采用回收站或软删除，并保留审计记录。
- 每日自动备份数据库和媒体，至少保留一个异地副本；定期执行恢复演练。
- 明确版权、转载、隐私、Cookie、评论规范和内容举报流程。

## 11. 测试要求

- 单元测试：权限判断、slug 生成、状态流转、文章过滤和数据校验。
- 集成测试：登录、文章发布、评论审核、媒体上传、搜索和订阅。
- 端到端测试：访客阅读链路、管理员发布链路和异常页面。
- 安全测试：越权、XSS、CSRF、SQL 注入、上传绕过和限流。
- 兼容性测试：主流桌面与移动浏览器、键盘导航、深色模式（如开启）。
- 发布前检查迁移脚本、备份可恢复性、404 链接、站点地图和 robots 配置。

## 12. 分阶段交付

### 阶段一：MVP

- 首页、文章详情、分类、标签、归档、搜索。
- 管理员登录、文章 CRUD、Markdown 编辑、媒体上传。
- SEO 基础信息、响应式布局、404 页面、数据库备份。

### 阶段二：运营能力

- 评论审核、RSS、订阅、文章版本、定时发布、统计看板。
- 角色权限、操作日志、图片优化、缓存和错误监控。

### 阶段三：增强能力

- 多作者协作、双因素认证、全文搜索引擎、国际化、多站点或多租户。
- 推荐系统、专题页、收藏、点赞、PWA 和更完整的内容工作流。

每个阶段发布前都应完成迁移、回滚、备份恢复和验收测试。

## 13. 验收标准

- 访客可以在手机和桌面端打开介绍页 `/`，进入博客首页 `/blog`，搜索并完整阅读一篇文章。
- 管理员可以创建草稿、预览、发布、下线和恢复文章。
- 分类、标签、图片和评论的新增、修改、删除行为符合权限规则。
- 未授权用户无法访问管理接口；恶意输入不能执行脚本或破坏数据。
- 文章 URL 稳定，站点地图、RSS、canonical 和分享卡片可正常生成。
- 读者可以复制文章链接、生成二维码，并生成包含标题、摘要、站点信息和二维码的分享海报；海报文字不溢出，二维码可扫码打开文章。
- 生产部署后，错误日志、备份任务和健康检查均有可验证结果。
- 新成员仅阅读本文即可理解页面范围、核心数据和上线前检查项。

## 14. 开发前需要确认的决策

- 这是个人博客、团队博客还是公开内容平台？是否需要多作者审核？
- 使用 Markdown、富文本还是两者兼容？是否需要导入已有文章？
- 评论、注册、订阅、点赞和收藏哪些功能必须首发？
- 文章和媒体的预计数量、访问峰值、预算及部署平台是什么？
- 是否需要多语言、深色模式、统计分析和自定义域名？
- 内容版权、隐私政策、备案或其他地区合规要求由谁负责？

## 15. 界面级功能规格

本章是实际开发和验收的细化依据。每个界面都应同时设计以下状态：首次加载、加载中、正常数据、空数据、无权限、网络失败、提交失败和操作成功。

### 15.1 全站通用框架

#### 顶部导航

| 元素 | 功能 | 结果 |
| --- | --- | --- |
| Logo/站点名 | 介绍页点击返回 `/`；博客内点击返回 `/blog` | 返回对应页面并回到页面顶部；博客内不重复打开展示页 |
| 导航菜单 | 进入文章、分类、归档、关于等页面 | 当前项高亮，浏览器地址同步更新 |
| 搜索按钮 | 展开搜索框或跳转搜索页 | 光标自动进入输入框，支持 `Esc` 关闭 |
| 主题切换 | 明暗主题切换 | 页面即时切换，刷新后保留偏好 |
| 订阅入口 | 打开订阅表单 | 成功后显示确认提示，重复邮箱显示已订阅 |

#### 页脚

- 展示版权年份自动更新、备案/隐私/用户协议链接、RSS、站点地图和社交链接。
- 博客页脚提供“站点介绍”链接返回 `/`，介绍页页脚提供“进入博客”链接进入 `/blog`。
- 外部链接默认新窗口打开并带 `noopener`；失效链接在发布检查中提示。

#### 全局反馈

- 保存、复制链接、订阅等短操作使用不遮挡内容的 Toast，2-4 秒后消失，并支持屏幕阅读器播报。
- 需要用户决定的操作使用对话框，明确主按钮、取消按钮和危险操作颜色。
- 表单错误显示在字段附近，保留用户已填写内容，不只显示一句“操作失败”。
- 网络请求超过 300ms 显示轻量加载状态，超过 3 秒显示“仍在加载”和重试入口。

### 15.2 博客首页 `/blog`

#### 页面结构

- 首屏：博客名称、内容导航、搜索入口和一篇主推文章；站点介绍和作者定位由根路径 `/` 介绍页承担。
- 内容区：最新文章列表（建议 10 条）、精选/热门区域、分类入口和归档入口。
- 侧栏（桌面端）：作者简介、订阅、热门标签；移动端移至文章列表之后。

#### 功能与结果

| 功能 | 操作 | 成功结果 | 异常/空状态 |
| --- | --- | --- | --- |
| 打开文章 | 点击标题、封面或“阅读全文” | 进入文章详情，保留返回位置 | 文章已下线时进入提示页 |
| 筛选最新文章 | 选择分类或标签 | 列表更新，筛选条件显示为可关闭的条件条 | 无文章时显示空插图、条件和“清除筛选” |
| 加载更多 | 点击按钮或滚动触发 | 在原列表下追加内容，按钮显示加载进度 | 无更多内容时按钮变为“已显示全部” |
| 订阅 | 输入邮箱并提交 | 显示“请查收确认邮件”；已确认则显示已订阅 | 邮箱格式错误、频率限制、服务不可用分别提示 |

#### 体验巧思

- 文章卡片显示预计阅读时间和最近更新时间，读者无需打开文章即可判断阅读成本。
- 从文章详情返回首页时恢复原滚动位置和筛选条件。
- 新访客只看到一次轻量订阅提示；关闭后至少 7 天不再打扰。
- 首屏主推文章使用稳定比例封面，避免图片加载造成布局跳动。

### 15.3 文章详情页 `/posts/:slug`

#### 页面结构

- 面包屑、标题、摘要、作者头像/名称、发布时间、更新时间、分类、标签。
- 正文区、文章目录（桌面端固定在侧边，移动端折叠）、阅读进度条。
- 文末操作区：点赞/收藏（如开启）、分享、版权声明、相关文章、上一篇/下一篇、评论。

#### 功能与结果

| 功能 | 操作 | 成功结果 | 异常/空状态 |
| --- | --- | --- | --- |
| 目录跳转 | 点击目录项 | 平滑滚动到对应标题，当前项高亮 | 标题过长时截断并提供完整悬浮提示 |
| 阅读进度 | 滚动正文 | 顶部进度条按正文比例更新 | 正文过短时不显示进度条 |
| 复制代码 | 点击代码块复制按钮 | 按钮短暂变为“已复制”，剪贴板写入原文 | 无剪贴板权限时选中文本并提示手动复制 |
| 图片查看 | 点击正文图片 | 打开全屏预览，支持缩放、左右切换、关闭 | 图片加载失败显示替代文本和重试 |
| 分享 | 点击分享 | 优先调用系统分享；不支持时显示复制链接、二维码 | 复制失败时显示可选择的链接文本 |
| 点赞/收藏 | 点击图标 | 数字即时更新，重复点击可撤销；未登录时引导登录或使用匿名策略 | 接口失败回滚数字并提示重试 |
| 评论 | 输入并提交 | 评论进入“待审核”或立即显示，表单清空 | 内容为空、超长、命中拦截词时就地提示 |
| 相关推荐 | 点击推荐卡片 | 进入新文章，推荐区域保留在历史记录中 | 无相关推荐时显示同分类最新文章 |

#### 体验巧思

- 目录仅在文章包含 2 个以上标题时显示；滚动时只高亮当前章节，避免视觉噪声。
- 离开未读完文章时记录进度，下次打开显示“继续阅读（约 X 分钟）”。
- 代码块默认只显示前 20 行，超出部分可展开，防止长代码淹没正文。
- 文章更新时在更新时间旁显示“内容有更新”，点击可查看版本摘要（如开启版本功能）。
- 复制链接时附带文章标题到系统剪贴板，方便直接粘贴分享。

#### 文章分享功能

分享是文章详情页的独立功能区，桌面端可放在标题下方和正文末尾，移动端建议在底部操作栏提供固定入口。分享按钮必须带有可识别的图标和文字/无障碍名称，不能只放一个没有提示的图标。

##### 分享入口与平台

| 入口/平台 | 操作 | 成功结果 | 不支持或失败时 |
| --- | --- | --- | --- |
| 系统分享 | 点击“分享”，浏览器支持 Web Share API 时调用系统分享面板 | 打开系统分享面板，分享标题、摘要和文章 URL | 降级为站内分享面板，不弹出无响应的空白窗口 |
| 复制链接 | 点击“复制链接” | 剪贴板写入规范化 URL；Toast 显示“链接已复制” | 无剪贴板权限时选中只读链接文本，并显示“请手动复制” |
| 微信/社交二维码 | 点击“二维码” | 弹窗展示当前文章二维码，支持下载 PNG | 生成失败显示重试；二维码弹窗可用 `Esc` 和关闭按钮退出 |
| 微信好友/朋友圈 | 在二维码弹窗中扫码 | 二维码内容包含文章 canonical URL，不增加一次性失效参数 | 说明“请使用微信扫一扫”，不伪造直接打开微信的成功状态 |
| 微博/X/Facebook 等 | 点击对应平台 | 新窗口打开平台分享地址，预填文章标题和 URL | 弹窗被拦截时提示允许弹窗，并提供复制链接 |
| 邮件分享 | 点击“发送邮件” | 打开默认邮件客户端，预填标题、摘要和链接 | 没有邮件客户端时显示可复制的主题和正文 |
| 分享到新标签页 | 右键或长按链接 | 链接具备真实 `href`，可被浏览器原生操作 | JavaScript 关闭时仍可正常访问文章 URL |

##### 分享内容规则

- 分享标题优先使用文章 SEO 标题，其次使用文章标题；标题过长时按平台限制截断并保留完整 URL。
- 分享摘要优先使用文章摘要，缺失时从正文提取前 120-160 个字符，去除 Markdown 标记和代码。
- 分享 URL 使用 canonical URL，去除后台预览参数和临时鉴权参数；如需统计，使用可配置的 UTM 参数，不改变 canonical。
- 复制文本默认格式为：`文章标题\n文章摘要\n文章 URL`，允许在设置中关闭摘要。
- 文章封面生成 Open Graph 图片，建议尺寸 1200x630；缺少封面时使用统一站点默认图，不能返回空白图片。

##### 分享面板交互

- 点击后打开轻量弹层，包含标题预览、封面缩略图、平台入口、复制链接、二维码和关闭按钮。
- 弹层打开后焦点进入弹层，按 `Esc` 关闭，点击遮罩关闭；关闭后焦点回到分享按钮。
- 复制、生成二维码等短操作不离开当前文章；外部平台在新窗口打开并标记为外部链接。
- 移动端弹层从底部滑出并避开系统安全区；桌面端使用居中的窄弹窗，不能遮挡正文关键内容。

##### 文章分享海报/介绍图

分享面板增加“生成分享图片”入口。该功能生成一张适合微信、朋友圈、社交平台和聊天工具直接转发的 PNG/JPEG 图片，图片本身必须包含足够的信息，即使脱离文章链接也能让接收者理解内容主题。

###### 图片内容

- 文章标题：必填，超长时按模板规则自动换行，最多 2-3 行；禁止文字溢出或压住二维码。
- 文章摘要：优先使用文章摘要，没有摘要时从正文提取 80-120 个字符；超长自动截断并显示省略号。
- 封面图或站点默认视觉：封面不存在、加载失败或不适合裁剪时使用默认图，不留下空白区域。
- 二维码：指向文章 canonical URL；二维码下方显示“扫码阅读全文”或自定义文案。
- 站点信息：Logo、站点名、作者名、发布时间（可配置）；不得显示后台 ID、草稿状态或敏感字段。
- 可选信息：文章分类/标签、预计阅读时间、作者头像、版权标识和短链接。

###### 推荐版式

| 模板 | 适用场景 | 版式要求 |
| --- | --- | --- |
| 横版卡片 | X、微博、聊天窗口预览 | 建议 1200x630，左侧标题摘要、右侧封面和二维码 |
| 竖版海报 | 微信朋友圈、手机长图 | 建议 1080x1440 或 1080x1920，上方视觉区、中部标题摘要、底部二维码 |
| 方形卡片 | 社交平台头像流、收藏 | 建议 1080x1080，标题区域与二维码保持清晰留白 |
| 极简文字版 | 无封面或低带宽场景 | 使用纯色背景和站点品牌色，标题、摘要、二维码仍必须完整 |

###### 生成与操作流程

1. 读者点击“生成分享图片”，弹出模板选择和图片预览；默认选择适合当前设备的平台模板。
2. 系统读取文章标题、摘要、封面、作者、站点配置并生成二维码；生成过程中显示骨架预览和进度状态。
3. 生成成功后提供“保存图片”“分享图片”“重新生成”“复制文章链接”四个操作。
4. 点击“保存图片”下载明确命名的文件，例如 `站点名-文章标题.png`；文件名中的非法字符自动替换。
5. 支持系统图片分享的设备调用原生分享面板并附带图片和文章 URL；不支持时仍可下载图片或复制链接。
6. 更换模板或点击“重新生成”只更新预览，不改变文章内容和文章 URL。

###### 生成结果与异常处理

| 场景 | 页面结果 |
| --- | --- |
| 正常生成 | 预览图清晰可放大，标题、摘要、二维码和站点信息完整；显示图片尺寸和文件大小 |
| 封面加载失败 | 自动切换默认背景并提示“已使用站点默认图片”，生成仍可继续 |
| 二维码生成失败 | 不允许生成一张不可用的海报，显示重试按钮和复制链接备用操作 |
| 浏览器不支持图片下载 | 打开图片原图新标签页，并提示长按/右键保存 |
| 浏览器不支持图片分享 | 隐藏或禁用“分享图片”，保留“保存图片”和“复制链接” |
| 标题/摘要超长 | 按字数和实际像素宽度裁切，保留标题开头和摘要结尾的完整标点，不出现重叠 |
| 网络中断 | 保留已经生成的预览；重新生成失败时不清空旧图，并显示重试 |
| 文章已下线 | 已生成的图片仍可保存，但二维码指向下线说明页，不指向后台预览地址 |

###### 视觉与可读性要求

- 二维码建议不小于 240x240 像素，四周保留足够空白区，保证手机扫码成功率。
- 标题与摘要使用高对比度颜色；图片背景复杂时增加不遮挡内容的半透明文字底板。
- 所有模板在 100% 和 200% 缩放下检查文字清晰度，不能把小字号版权信息作为主要信息。
- 生成图的文字使用服务端或可控字体渲染，避免用户设备缺少字体导致换行错乱。
- 不使用文章正文中的第三方图片作为不可控背景，防止版权、跨域或加载失败影响生成。

###### 后台配置与数据

- 管理员可设置默认模板、可用模板、品牌色、Logo、默认背景图、二维码 Logo、底部文案和版权信息。
- 管理员可为单篇文章指定分享图标题、摘要、背景图和二维码文案；留空时继承全站默认值。
- 后台预览必须使用真实文章数据，并提供长标题、无封面和中文摘要三种测试样例。
- 生成图片可采用即时生成或短期缓存；缓存键至少包含文章版本、模板和分享配置版本，文章更新后不能继续使用旧标题。
- 记录生成成功/失败次数和模板使用次数，但不记录访客身份或二维码扫描者的个人信息。

###### 分享海报体验巧思

- 预览旁显示“扫码测试”提示，管理员和作者发布前可以用手机快速验证二维码。
- 在海报底部保留短链接文本，二维码因压缩或截图无法识别时仍有可输入的备用地址。
- 生成完成后提供“带水印/无水印”选项（按站点策略），默认不遮挡标题、摘要和二维码。
- 记住用户上次选择的模板，但只在本地保存模板偏好，不保存文章正文或敏感配置。
- 对朋友圈竖版海报默认隐藏过长的标签，优先保证标题、摘要和二维码的阅读与识别。

##### 分享统计与隐私

- 可选记录分享点击量，至少区分文章、平台、日期和设备类型；默认不记录分享者姓名、通讯录或社交账号。
- 统计点击应采用匿名聚合和限流，不能因为点击分享就加载非必要的第三方追踪脚本。
- 后台文章详情显示“分享次数”和平台分布，并注明统计周期、数据延迟和去重口径。
- 用户拒绝统计 Cookie 时，分享仍可用；拒绝只影响统计，不影响复制、二维码和系统分享。

##### 分享后的页面表现

- 通过平台抓取链接时返回正确的 `title`、`description`、`og:title`、`og:description`、`og:image`、`og:url` 和 `twitter:card`。
- 分享卡片图片必须有明确的背景、文章标题和站点标识，标题过长时自动换行，不出现文字溢出。
- 文章下线或删除后，旧分享链接进入 404/下线说明页，并提供首页、搜索和相关文章入口。
- 文章 slug 变更后旧链接使用 301 重定向，分享卡片仍指向新的 canonical URL。

##### 分享体验小巧思

- 读者点击复制后，在按钮旁短暂显示勾选图标和“已复制”，而不是突然弹出遮挡内容的提示。
- 分享面板优先显示读者当前设备最可能使用的方式：手机优先系统分享和二维码，桌面优先复制链接和二维码。
- 文章读到 80% 后才在正文末尾强调分享入口；首次进入页面不立刻弹窗，减少打断。
- 分享二维码旁显示文章短标题和“扫码继续阅读”，方便截图后识别来源。
- 对失败的外部平台跳转保留一个明确的备用动作“复制链接”，让分享流程始终可完成。
- 分享按钮显示当前文章的分享次数时，使用简短格式（如 `1.2k`），悬停或点击再查看准确数字和统计周期。

### 15.4 分类页 `/categories` 与分类详情 `/categories/:slug`

#### 页面结构

- 分类页展示分类名称、简介、文章数量和最近更新时间。
- 分类详情展示分类介绍、筛选后的文章列表、分页和子分类切换。

#### 功能与结果

- 点击分类卡片进入详情页；文章数量为 0 时仍保留分类说明，并提示“暂无文章”。
- 切换子分类时使用 URL 参数同步状态，刷新或分享链接后仍能得到相同列表。
- 分页使用可索引 URL；翻页后页面滚动到列表标题而不是顶部导航。

#### 体验巧思

- 分类卡片显示代表性文章标题，而不是只显示数字，帮助读者快速判断内容方向。
- 文章数量和“最后更新”并列展示，避免空分类看起来像系统故障。

### 15.5 标签页 `/tags` 与标签详情 `/tags/:slug`

- 标签总览使用按字母/热度排序的紧凑列表，显示文章数量。
- 标签详情复用文章列表组件，但标题区域显示标签描述和相关标签。
- 合并标签后旧标签 URL 返回 301 到新标签；删除标签前要求选择“迁移到其他标签”或“移除关联”。
- 标签为空时显示创建时间和“尚未有文章”，管理员可以从此处直接编辑。

### 15.6 归档页 `/archives`

- 左侧/顶部提供年份选择，主体按年份和月份分组列出标题、日期、分类和阅读时长。
- 点击月份可折叠/展开；默认展开最近一年，其余年份折叠。
- 年份不存在时显示可用年份列表，不返回空白页面。
- 提供“随机阅读一篇”入口，点击后从已发布文章中跳转，并排除当前已访问文章（可选）。

### 15.7 搜索页 `/search`

#### 页面结构

- 搜索框、清空按钮、搜索范围筛选（全部/标题/标签/分类）、排序（相关性/最新）。
- 结果总数、搜索耗时（可选）、结果列表和分页。

#### 功能与结果

- 提交后 URL 使用 `?q=` 保存关键词，支持回车、点击按钮和浏览器前进后退。
- 关键词最少 1 个、最多 100 个字符；连续空格合并，危险字符转义。
- 结果中高亮标题和摘要匹配片段；点击结果进入文章详情。
- 无结果显示“没有找到相关内容”、推荐关键词、分类入口和清除按钮。
- 搜索服务失败显示已有关键词、明确错误原因和重试按钮，不伪装成无结果。

#### 体验巧思

- 输入时展示最近搜索（仅本地保存，最多 5 条），提供一键删除，不上传无效关键词。
- 对常见错别字提供“是否搜索：X”的建议，但不自动替换用户输入。

### 15.8 作者页 `/authors/:id`

- 展示作者头像、姓名、简介、擅长领域、社交链接和文章数。
- 下方按最新/最受欢迎切换文章列表；切换结果同步 URL。
- 作者被禁用或不存在时，文章仍可按站点策略保留作者名，但作者页显示不可用提示。
- 体验细节：作者头像、昵称、简介使用统一占位骨架，避免图片失败导致版式变化。

### 15.9 关于页、联系页、隐私页

#### 关于页

- 展示作者/团队介绍、站点发展时间线、技术栈、联系方式、版权和转载规则。
- 时间线无内容时隐藏区块，而不是显示空标题。

#### 联系页

- 字段：姓名/昵称、邮箱、主题、正文、附件（可选）、验证码和隐私同意。
- 提交成功显示工单编号和预计回复时间；提交失败保留正文并提供重试。
- 防重复提交：提交按钮在请求期间禁用，成功后在短期内拦截相同内容。

#### 隐私政策/用户协议

- 顶部显示生效日期和版本，正文标题生成目录。
- 更新后提示用户查看变更摘要；历史版本可访问（合规需要时）。

### 15.10 404、403、500 和维护页

- 404：说明页面不存在，提供搜索框、返回博客首页 `/blog`、热门文章和上一级路径。
- 403：说明权限不足，提供登录入口和返回来源页，不泄露资源是否存在的敏感信息。
- 500：显示错误编号、重试、返回博客首页 `/blog` 和联系入口；错误编号写入日志用于排查。
- 维护页：显示预计恢复时间、状态页链接和订阅恢复通知入口。
- 所有错误页保持站点品牌和导航，但不依赖可能已经故障的接口。

## 16. 管理端界面级功能规格

### 16.1 管理员登录页 `/admin/login`

- 字段：账号/邮箱、密码、记住设备（可选）、验证码（触发后显示）、登录按钮。
- 登录成功：创建会话并跳转原目标页；无目标时进入仪表盘。
- 登录失败：账号不存在、密码错误和账号锁定使用不泄露账号存在性的统一提示；记录失败次数。
- 超时/失效：回到登录页并说明“会话已过期”，登录后回到原页面。
- 小巧思：密码框提供显示/隐藏，提交按钮显示明确进度；浏览器自动填充和键盘回车可用。

### 16.2 管理端通用布局

- 左侧导航：仪表盘、内容、媒体、评论、用户、设置、日志；显示待处理数量徽标。
- 顶部：面包屑、全局搜索、任务状态、当前用户菜单。
- 列表页统一支持筛选、排序、分页、批量操作、列显示配置和导出（按权限）。
- 任何危险操作都显示影响范围，例如“将删除 3 篇文章及其 12 个关联草稿引用”。
- 离开未保存页面时拦截导航，提供“继续编辑/放弃修改”选择。

### 16.3 仪表盘 `/admin`

#### 页面结构

- 指标卡：已发布文章、待审核评论、草稿、近 7 日访问量、错误数。
- 趋势图：访问量、发布量、评论量，可切换 7/30/90 天。
- 待办列表：待审核评论、即将发布文章、失败任务、备份状态。
- 热门文章表格：标题、访问量、平均阅读时长、更新时间。

#### 功能与结果

- 切换时间范围后所有图表和同比数据同步更新，并显示数据时间范围。
- 点击指标卡进入对应筛选后的列表页。
- 统计接口失败时保留页面结构，显示“数据暂不可用”和重试，不阻断文章管理。
- 首次使用无数据时显示三步引导：创建分类、发布第一篇文章、配置站点信息。

#### 体验巧思

- 待办按紧急程度排序，而不是只按时间；备份失败置顶并提供直接处理入口。
- 指标卡显示与上一周期的变化趋势，但同时显示统计口径和采样时间。

### 16.4 文章列表 `/admin/posts`

#### 页面结构

- 顶部：新建文章、状态筛选、作者筛选、分类/标签筛选、关键词搜索、日期范围。
- 表格列：复选框、标题、状态、作者、分类、标签、更新时间、发布时间、阅读量、操作。
- 行操作：编辑、预览、复制、发布/下线、移入回收站。

#### 功能与结果

- 筛选、排序和分页写入 URL，刷新后保持。
- 批量操作前显示选中数量和受影响文章；完成后显示成功/失败明细。
- 发布前校验标题、slug、正文、分类、封面 alt 和 SEO 必填项；失败时定位到对应字段。
- 回收站支持恢复和永久删除，永久删除需要输入文章标题确认。
- 无文章时显示新建按钮和导入入口；无筛选结果时显示清除筛选。

### 16.5 文章编辑器 `/admin/posts/new`、`/admin/posts/:id/edit`

#### 页面结构

- 顶部：返回列表、保存状态、预览、保存草稿、提交审核、发布菜单。
- 主编辑区：标题、正文编辑器、摘要、封面、分类、标签。
- 右侧设置：slug、发布时间、作者、置顶、评论开关、SEO 标题/描述/canonical。
- 底部状态：字数、预计阅读时间、最后保存时间、版本入口。

#### 功能与结果

| 功能 | 操作 | 结果 |
| --- | --- | --- |
| 自动保存 | 输入停止 1 秒后触发 | 显示“保存中/已保存/保存失败”，失败可重试且不清空编辑内容 |
| 预览 | 点击预览 | 新窗口或分屏展示真实发布样式，预览不改变发布状态 |
| 草稿保存 | 点击保存 | 保留编辑页，生成可追溯版本并更新保存时间 |
| 提交审核 | 点击提交 | 状态变为审核中，作者不可修改受限字段（按权限） |
| 定时发布 | 选择未来时间 | 显示计划任务状态，到时发布；时区明确显示 |
| 发布 | 点击发布并确认 | 状态变为已发布，生成 canonical、sitemap 更新任务和成功链接 |
| 恢复版本 | 选择历史版本 | 先载入对比预览，确认后写入新版本，不覆盖历史记录 |
| 上传图片 | 拖拽或选择文件 | 上传进度、压缩结果、alt 文本填写和插入位置明确显示 |

#### 体验巧思

- 编辑器快捷键、Markdown 快捷输入和工具栏保持一致；工具栏按钮有 tooltip 与快捷键。
- 粘贴大段内容时提示清理格式；粘贴图片自动上传但显示可取消的队列。
- 标题旁实时显示 slug 预览，手动修改后不再随标题变化，避免已发布链接被意外改写。
- 发布前显示“发布检查清单”，每项可一键定位；只有阻断项会阻止发布。
- 正文预览使用真实字体、代码样式和图片比例，减少“编辑器看起来正常、发布后变形”。

### 16.6 文章预览与版本页

- 预览页顶部显示“仅管理员可见”和当前草稿时间，提供返回编辑和发布按钮。
- 版本页按时间列出版本、编辑者、变更摘要；支持双栏差异查看。
- 恢复前展示新增、删除、修改统计；恢复动作创建新版本并写入审计日志。

### 16.7 分类与标签管理页

- 分类支持树形拖拽排序、展开/折叠、父子级编辑和批量启用/隐藏。
- 标签支持搜索、合并、批量删除和文章数预览。
- 保存成功：列表局部更新并保留筛选；名称/slug 重复：字段旁明确指出冲突记录。
- 移动分类可能改变 URL 或面包屑时，提交前显示影响文章数量和重定向策略。

### 16.8 媒体库 `/admin/media`

#### 页面结构

- 上传区、类型筛选、关键词搜索、排序、网格/列表切换。
- 预览抽屉：大图/播放器、文件信息、alt 文本、标题、引用文章、复制 URL、替换和删除。

#### 功能与结果

- 上传支持多文件队列，每项显示进度、成功 URL、失败原因和重试。
- 删除前显示引用数量；被文章引用时默认禁止直接删除，提供替换或解除引用入口。
- 图片编辑 alt 后即时保存；未填写 alt 的图片在发布检查和无障碍报表中标记。
- 大文件超过限制时在客户端先提示，服务端再次校验。

#### 体验巧思

- 从文章编辑器打开媒体库时，关闭抽屉后自动把选中的媒体插入原光标位置。
- 网格缩略图固定比例，图片加载采用模糊占位，避免布局抖动。

### 16.9 评论管理 `/admin/comments`

- 默认进入待审核队列，支持全部/待审核/已通过/垃圾/举报筛选。
- 每行显示文章、评论摘要、昵称、时间、风险标记、状态和操作。
- 操作：通过、拒绝、标记垃圾、回复、编辑、删除、查看上下文。
- 批量通过后显示成功数量和失败项；误操作可在短时间内撤销。
- 评论内容疑似垃圾时显示命中规则，不直接暴露内部敏感策略给访客。

### 16.10 用户与角色 `/admin/users`

- 用户列表支持状态、角色、注册时间和最近登录筛选。
- 用户详情显示资料、文章、评论、会话和操作记录，但敏感字段默认脱敏。
- 禁用用户后立即使其会话失效，并显示影响范围；重新启用不自动恢复已删除内容。
- 修改角色需二次确认；最后一个管理员不能被降级或禁用。

### 16.11 站点设置 `/admin/settings`

- 使用分组页签：基础信息、导航、文章、评论、SEO、邮件、统计、存储、安全。
- 每组显示字段说明、示例和当前生效值；敏感密钥只显示末尾字符。
- 保存支持单组提交，成功后显示变更摘要；需要重建缓存/站点地图时显示后台任务进度。
- 未保存离开时拦截；恢复默认值先展示将改变的字段列表。
- 分享设置单独提供：启用/关闭分享平台、默认分享摘要模板、二维码 Logo、默认分享图、UTM 参数、是否统计分享点击。
- 设置页提供分享卡片实时预览：标题过长、无封面、中文换行和默认图都必须可预览。
- 关闭某个平台后，文章详情页立即隐藏该入口；复制链接和系统分享等基础能力不受影响。
- 保存分享设置后显示生效范围（全部文章/仅新文章）和缓存刷新状态，避免管理员误以为已立即刷新所有平台缓存。

### 16.12 操作日志、任务与备份

- 操作日志支持操作者、动作、目标类型、时间、IP、结果筛选和详情查看。
- 任务页展示发布、图片处理、邮件、索引、站点地图等异步任务的排队/执行中/成功/失败状态。
- 备份页显示最近备份时间、大小、校验结果、保留期限和恢复演练记录。
- 失败任务提供错误摘要、重试按钮和关联日志；重试应使用幂等任务 ID，避免重复发布或重复发信。

## 17. 统一状态与交互验收规则

### 17.1 状态定义

- `loading`：首次请求或切换筛选时显示骨架，不闪烁整页。
- `empty`：请求成功但没有数据，说明原因并给出下一步操作。
- `error`：请求失败，说明是否可以重试；保留用户输入和当前筛选。
- `success`：明确说明完成了什么，必要时提供查看结果或撤销入口。
- `disabled`：说明禁用原因，不能只置灰无解释。

### 17.2 表单规则

- 必填字段在提交时统一校验，输入过程中只做轻量格式提示。
- 错误消息使用“如何修正”的表达，例如“slug 已存在，请换一个或使用系统建议值”。
- 提交按钮在请求期间不可重复点击；服务端必须使用幂等键防止重复创建。
- 服务端错误使用用户可理解的摘要，详细错误写入日志，不直接展示堆栈。

### 17.3 列表规则

- 筛选、排序、分页、批量选择均可通过键盘完成，并在移动端转换为抽屉筛选。
- 批量操作完成后不跳回第一页，除非当前页已无数据。
- 删除、下线、禁用等危险操作必须说明后果；成功后支持撤销或进入回收站。

### 17.4 移动端规则

- 文章阅读优先于装饰，正文字号、行高和点击区域保持舒适。
- 管理表格转为卡片/分组列表，核心字段保留，次要字段放入详情抽屉。
- 底部固定操作栏不得遮挡正文和系统安全区；键盘弹出时表单按钮仍可见。

## 18. 体验小巧思清单

以下细节应作为可评估的产品任务，而不是临时装饰：

- **阅读进度记忆**：本地记录文章阅读百分比，回访时提供继续阅读入口；不记录正文内容。
- **预计阅读时间**：按中文字符和代码块分别估算，显示“约 6 分钟”，发布后固定，避免每次变化。
- **链接健康提示**：编辑器中检测明显失效的站内链接，发布前提示但允许忽略。
- **草稿安全网**：自动保存失败时保留本地草稿，恢复时显示本地版本时间，避免覆盖服务器版本。
- **分享优化**：生成带标题的复制文本、二维码和社交卡片预览，读者能看到分享后的效果。
- **分享海报**：生成包含标题、摘要、封面/默认背景、站点信息和二维码的图片，可保存到设备或通过系统图片分享。
- **低干扰订阅**：文章读完后再显示订阅入口；已订阅用户不再显示重复提示。
- **上下文返回**：记录列表筛选、滚动位置和搜索词，返回时恢复，不让读者重新寻找。
- **渐进增强**：JavaScript 失败时仍能阅读文章、访问分页链接和提交基础导航。
- **可解释统计**：每个数字旁提供统计周期、来源和去重口径，避免误导作者。
- **发布前安全感**：发布按钮旁显示检查结果，例如“正文 1,240 字、3 张图片、2 个未填写 alt”，让作者知道发布内容的完整度。
- **尊重注意力**：避免自动播放、强制弹窗和连续提示；每一种提示都提供永久关闭或明确频率控制。

## 19. 按界面的验收用例模板

开发每个界面时至少填写以下用例，测试和设计评审共用：

| 用例 | 前置条件 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| 正常路径 | 有可用数据、网络正常 | 完成主要操作 | 页面状态、URL、数据库和提示均正确 |
| 空数据 | 查询结果为 0 | 打开页面或应用筛选 | 显示原因和下一步按钮，不显示空白页 |
| 输入错误 | 必填项为空或格式错误 | 提交表单 | 字段级错误，用户输入不丢失 |
| 权限不足 | 使用低权限账户 | 访问页面或执行操作 | 页面/按钮按权限隐藏或禁用，接口返回 403 |
| 重复操作 | 快速连续点击 | 重复提交 | 只创建一条记录，按钮有进行中状态 |
| 网络失败 | 请求超时或服务异常 | 提交/加载 | 显示可理解错误和重试，页面其余内容可用 |
| 恢复操作 | 存在可恢复数据 | 删除后点击撤销/恢复 | 数据恢复，列表和提示同步更新 |
| 移动端 | 375px 宽视口 | 完成同样操作 | 无横向滚动、无遮挡、触控区域足够大 |

## 20. 细化后的交付优先级

### P0：必须首发

- 介绍页、博客首页、文章详情、搜索、分类/标签、归档、关于、错误页。
- 管理登录、文章编辑/预览/发布、媒体上传、分类标签管理。
- 响应式、基础 SEO、复制链接、二维码、分享海报基础模板、表单校验、权限控制、备份和错误日志。

### P1：上线后尽快补齐

- 评论审核、RSS、订阅、版本历史、定时发布、站点统计、操作日志。
- 阅读进度、代码复制、图片预览、上下文返回、发布前检查清单、多个分享海报模板和后台海报配置。

### P2：规模化后再做

- 多作者工作流、双因素认证、全文搜索引擎、国际化、收藏/点赞、PWA。
- 链接健康检测、推荐系统、自动摘要、内容质量报表和多站点能力。
