spring:
  datasource:
    url: jdbc:h2:mem:signal_notes;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
app:
  admin:
    username: admin
    password: signal2026
  cors-origins: http://127.0.0.1:5173
  upload-dir: ./target/test-uploads
  backup-dir: ./target/test-backups
